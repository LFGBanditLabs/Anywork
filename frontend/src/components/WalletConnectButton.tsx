"use client";

// Simple wrapper around the Reown AppKit connect button web component.
// AppKit is initialised in AppKitProvider (src/app/AppKitProvider.tsx).

export const WalletConnectButton = () => {
  return (
    <appkit-button />
  );
};

export const projectId = process.env.NEXT_PUBLIC_PROJECT_ID || "";

// Reown/AppKit is now initialized via AppKitProvider (src/app/AppKitProvider.tsx).
// This file is kept as a lightweight placeholder so any legacy imports do not crash.
